<%- model_class = Post -%>

<%= render 'layouts/header', h1: "", span: "Chuyên mục: " << Category.find(@post.category_id).name, h4: @post.title%>

  <!-- Post Content -->
  <article>
    <div class="container">
      <div class="row">
        <div class="col-lg-8 col-md-10 mx-auto">
            <%=@post.content%>
        </div>
      </div>
    </div>
  </article>

  <hr>

<%= render 'layouts/icon'%>
