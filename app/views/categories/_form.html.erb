<%= form_with model: category do |form|%>

    <% if @category.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@category.errors.count, "error") %> prohibited this post from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @category.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <div class="input-group">
      <%= form.text_field :name,
      class: "form-control", 
      placeholder: "name" %>
    </div>
  </div>

  <div class="actions">
    <%= form.submit "Tạo mới danh mục", class: "btn btn-success btn-block" %>
  </div>

    <% if category.errors.any? %>
        <% category.errors.each do |error| %>
        <div category="alert alert-danger" role="alert">
        <%= category.full_message %> 
        </div>
        <% end %>
  <% end %>

<%end%>